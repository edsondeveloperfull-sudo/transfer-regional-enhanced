{
  "name": "transferencias-regionais",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "http-server -c-1 -p 8080",
    "test": "jest --config=jest.config.cjs",
    "lint": "eslint . --ext .js,.html",
    "e2e": "npm run start & sleep 1 && npx playwright test --project=chromium",
    "k6:run": "k6 run k6/load-test.js || echo \"Install k6 to run performance tests\""
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "eslint": "^8.50.0",
    "http-server": "^14.1.1",
    "jest": "^29.6.0"
  }
}